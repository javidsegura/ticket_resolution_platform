[pytest]
asyncio_mode = auto
# Integration tests run against the app via ASGI transport, not a test database
# Tests validate E2E behavior of the MVP CSV upload pipeline
markers =
    integration: marks tests as integration tests (deselect with '-m "not integration"')
    e2e: marks tests as end-to-end tests
